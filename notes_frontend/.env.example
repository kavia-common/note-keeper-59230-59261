# Notes Frontend environment example
# Copy to .env and adjust values as needed (the platform will map these appropriately).
# Do not commit actual secrets.

# Base URL for the Notes REST API (e.g., https://api.example.com)
NG_APP_API_BASE_URL=/api

# Toggle mock API usage (true or false)
NG_APP_USE_MOCK_API=true
