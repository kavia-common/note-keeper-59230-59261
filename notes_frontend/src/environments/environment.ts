export const environment = {
  production: false,
  apiBaseUrl: '/api', // PUBLIC endpoint base; replace with actual backend URL when available
  useMockApi: true,   // Toggle to false when backend is ready
};
